Run the included helper to add, commit and push workspace changes.

Windows (CMD):

```bat
cd "C:\Users\Al-inglity\Downloads\site codeflow\Code-Flow-Visualizer"
save-and-push.bat
```

Git Bash / WSL:

```bash
cd "/c/Users/Al-inglity/Downloads/site codeflow/Code-Flow-Visualizer"
./save-and-push.sh
```

Notes:
- `git push` may prompt for credentials if your remote requires authentication.
- If you restart the machine, run the script after login to push the changes.
